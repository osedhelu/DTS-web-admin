# URL pública del backend Django (incluye /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# ── Railway (producción) ─────────────────────────────────────────────────────
# Guía completa: DEPLOY_RAILWAY.md (este directorio)
#
# En el servicio DTS-web-admin (dentro de Railway):
#   NEXT_PUBLIC_API_URL=https://${{DTS-backend.RAILWAY_PUBLIC_DOMAIN}}/api/v1
#
# También actualiza en DTS-backend:
#   WEB_URL=https://${{DTS-web-admin.RAILWAY_PUBLIC_DOMAIN}}
